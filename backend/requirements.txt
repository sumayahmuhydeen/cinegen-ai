# CineGen AI — Backend Dependencies
# Windows-compatible version (Phase 2)

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database (SQLite for local dev, PostgreSQL for production)
sqlalchemy==2.0.30
alembic==1.13.1

# NOTE: psycopg2-binary is for PostgreSQL (production only)
# Do NOT install on Windows for local dev — we use SQLite locally
# psycopg2-binary==2.9.9  <-- commented out for Windows

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1
annotated-types==0.7.0

# API utilities
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client
httpx==0.27.0

# Anthropic (Script Intelligence)
anthropic==0.28.0

# Environment variables
python-dotenv==1.0.1

# Async support
anyio==4.4.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
